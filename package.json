{
  "name": "ng-trello",
  "private": true,
  "version": "0.0.0",
  "description": "An angular project for Trello like application",
  "repository": "https://github.com/achyutjhunjhunwala/aj-trello",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
